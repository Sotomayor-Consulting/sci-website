---
import HeroB from "@/components/ui/blocks/HeroB.astro";
import Form from "@components/ui/blocks/FormB.astro";
import Insight1 from "@components/ui/blocks/Insights_4.astro";
import Insight2 from "@components/ui/blocks/Insights_5.astro";
import Pricing, {
  type PricingPlan,
} from "@components/ui/blocks/PricingB.astro";
import Faq from "@components/ui/blocks/FaqB.astro";
// import Icon from "node_modules/astro-icon/components/Icon.astro";

const activityGroups = [
  {
    label: "Operar un negocio",
    activities: [
      "Exportación de servicios profesionales",
      "E-commerce",
      "Trader / compra-venta de productos",
      "Fintech",
    ],
  },
  {
    label: "Invertir o proteger",
    activities: [
      "Invertir en Bolsa de Valores",
      "Trader de Stocks",
      "Negocios en bienes raíces",
      "Proteger mi patrimonio",
    ],
  },
];

const benefits = [
  ["30 minutos", "Por Zoom, sin compromiso"],
  ["Sin visa ni residencia", "Sin SSN ni ITIN"],
  ["100% remoto", "Atención en español"],
];

const steps1 = [
  {
    titulo: "Cuéntanos tu caso",
    descripcion: "Comparte tus datos y el objetivo principal de tu proyecto.",
  },

  {
    titulo: "Habla con un experto.",
    descripcion:
      "Revisamos actividad, socios, residencia y planes de crecimiento.",
  },
  {
    titulo: "Recibe una ruta clara",
    descripcion: "Sales con estado recomendado, costos y próximos pasos.",
  },
];

const steps2 = [
  {
    titulo: "Delaware",
    descripcion:
      "Prestigio corporativo. La opción cuando hay inversionistas de por medio.",
  },

  {
    titulo: "Wyoming",
    descripcion: "Menor costo operativo y confidencialidad total.",
  },
  {
    titulo: "Florida",
    descripcion:
      "Para expandirse con presencia física y para productos que llegan por barco desde cualquier parte del mundo.",
  },
  {
    titulo: "Tu caso",
    descripcion:
      "El estado correcto depende de tu actividad, tus socios y de cómo cobras. Lo definimos contigo en la asesoría inicial, antes de que inviertas.",
  },
];

const steps3 = [
  {
    titulo: "Tu actividad",
    descripcion: "Revisamos a qué te dedicas y cómo cobras hoy.",
  },

  {
    titulo: "Tu estructura",
    descripcion: "Socios, residencia fiscal y planes de crecimiento.",
  },
  {
    titulo: "Tu estado",
    descripcion: "Comparamos las opciones y su encaje con tu operación.",
  },
  {
    titulo: "Tu ruta",
    descripcion: "Sales con una recomendación concreta y los costos asociados.",
  },
];

import HeromImage from "@images/Hero_Sotomayor_3.avif";

const planes: PricingPlan[] = [
  {
    name: "Básico",
    description: "Constituye tu LLC con lo esencial para operar legalmente.",
    price: "$350",
    period: "Una vez",
    features: [
      {
        text: "Asesoría para incorporar tu LLC ante el estado que elijas.",
        included: true,
      },
      {
        text: "Acompañamiento con los documentos corporativos de tu LLC.",
        included: true,
      },
      { text: "Agente Residente por un año.", included: true },
    ],
    buttonText: "Agenda un cita",
    buttonHref: "/tu-llc-en-usa/#agendar",
  },
  {
    name: "Business",
    description:
      "Estrategia fiscal y legal para la incorporación de tu LLC. Ideal para escalar y tener bien estructurado desde el inicio.",
    price: "$950",
    period: "Una vez",
    features: [
      {
        text: "Incluye todo del Plan Básico + Estándar",
        included: true,
      },
      { text: "Planificación y diseño legal - tributario.", included: true },
      {
        text: "Acompañamiento bancario (Mercury o Relay + Stripe).",
        included: true,
      },

      {
        text: "Acompañamiento prioritario con los documentos corporativos de tu LLC.",
        included: true,
      },
    ],
    buttonText: "Agenda un cita",
    buttonHref: "/tu-llc-en-usa/#agendar",
    highlighted: true,
    badge: "Más Popular",
  },
  {
    name: "Estándar",
    description: "Incluye soporte legal y documentos clave para operar.",
    price: "$600",
    period: "Una vez",
    features: [
      {
        text: "Incluye todo del Plan Básico",
        included: true,
      },
      { text: "Elaboración del Acuerdo de Operación.", included: true },
      { text: "Consultas ilimitadas por un año.", included: true },
      {
        text: "Asesoría sobre la exención BE-13",
        included: true,
      },
    ],
    buttonText: "Agenda un cita",
    buttonHref: "/tu-llc-en-usa/#agendar",
  },
];

const faqs = [
  {
    question: "¿Sotomayor Consulting es una entidad del Gobierno?",
    answer:
      "No. Somos una firma privada de asesoría legal, fiscal y tecnológica. El registro de la LLC lo emite el Estado y el EIN lo emite el IRS; nosotros te asesoramos y acompañamos en el proceso.",
  },
  {
    question: "¿La asesoría realmente es gratuita?",
    answer:
      "Sí. La reunión inicial no tiene costo ni compromiso. Primero evaluamos si una LLC encaja con tu situación.",
  },
  {
    question: "¿Puedo abrir una LLC sin vivir en Estados Unidos?",
    answer:
      "Sí. Una persona no residente puede constituir una LLC. La estructura y obligaciones adecuadas dependen de su actividad y país de residencia.",
  },
  {
    question: "¿Cuánto tarda el proceso de incorporación de LLC?",
    answer: "El proceso de incorporación dura aproximadamente: hasta 15 días",
  },
  {
    question: "¿Qué documentos necesito?",
    answer:
      "Normalmente necesitarás pasaporte vigente y comprobante de domicilio reciente de cada socio.",
  },
  {
    question: "¿Qué incluye el costo?",
    answer:
      "Cada plan incluye servicios distintos. La tasa estatal se paga por separado y varía según la jurisdicción.",
  },
];
---

<main
  class="relative min-h-screen overflow-hidden bg-[#0d2537] text-white"
  data-llc-onboarding-b
>
  <div
    class="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_72%_12%,rgba(216,167,79,.18),transparent_28%),radial-gradient(circle_at_18%_84%,rgba(50,105,137,.34),transparent_35%),linear-gradient(115deg,#081d2c_0%,#0d2a3e_48%,#183f58_100%)]"
  >
  </div>
  <div
    class="pointer-events-none absolute inset-0 [background-image:linear-gradient(rgba(255,255,255,.06)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,.06)_1px,transparent_1px),var(--hero-image)] [background-size:54px_54px,54px_54px,cover] [background-position:0_0,0_0,center] [background-repeat:repeat,repeat,no-repeat] opacity-10"
    style={`--hero-image: url('${HeromImage.src}');`}
  >
  </div>

  <div
    class="relative z-10 grid min-h-screen xl:grid-cols-[minmax(0,1fr)_36.625rem]"
  >
    <HeroB
      badge="Asesoría estratégica gratuita"
      title="Tu LLC en Estados Unidos, "
      titleSpan="bien hecha"
      description="Te guiamos para evaluar el estado adecuado para tu LLC y estructurarla
          con criterios legales, fiscales y bancarios, reduciendo riesgos."
      buttonLink="#agendar-b"
      button="Agendar "
      advice="SCI es una consultora privada e independiente: no es una agencia gubernamental ni está afiliada o respaldada por una. Brindamos orientación, preparación documental y acompañamiento; la autoridad competente decide la emisión y los plazos. "
      benefits={benefits}
    />

    <Form activityGroups={activityGroups} />
  </div>
  <div class="relative z-10 my-10 bg-black/10 py-20">
    <Insight1
      label="Cómo funciona"
      title="De la duda a un plan concreto en tres pasos."
      Cta="Agendar mi asesoría"
      CtaLink="#agendar"
      CtaLabels="30 min · por Zoom · sin compromiso"
      steps={steps1}
    />
  </div>

  <div class="relative z-10 my-10 py-20">
    <Insight2
      label="Elige tu estado ideal"
      title="Delaware, Wyoming o Florida. No todos los estados son iguales para una incorporación."
      description="Recibe la consultoría de un equipo experto y define el estado que corresponde a tu operación antes de incorporar tu LLC, sin errores. Diez años estructurando compañías en Estados Unidos para clientes de toda América Latina."
      Cta="Evalúa tu estado sin costo"
      CtaLink="#agendar"
      CtaLabels="30 min · por Zoom · sin compromiso"
      CtaAdvice="Las tasas estatales las cobra cada estado y se pagan por separado."
      steps={steps2}
    />
  </div>

  <div class="relative z-10 my-10 bg-black/10 py-20">
    <Insight2
      label="Consultoría para tu LLC"
      title="Analizamos tu caso y tu actividad antes de recomendarte una estructura."
      description="Una sesión de 30 minutos por Zoom, sin costo y sin compromiso, con un asesor del estudio. Diez años de experiencia en estructuras internacionales."
      Cta="Agenda una llamada sin costo"
      CtaLink="#agendar"
      CtaLabels="100% remoto y en español"
      steps={steps3}
    />
  </div>

  <div class="relative z-10 my-10 py-20">
    <Pricing
      heading="Valores únicos. Las tasas del Estado se pagan aparte y las cobra el Estado."
      description="El plan ideal según tu caso lo definimos en la primera asesoría."
      plans={planes}
    />
  </div>

  <div class="relative z-10 my-10 py-20">
    <Faq
      label="Preguntas frecuentes"
      title="Lo que debes saber antes de empezar."
      description="Si tu duda no aparece, la resolvemos durante la asesoría."
      faqs={faqs}
      Cta="Resuleve tus dudas en la asesoría"
      CtaLink="#agendar"
      CtaLabels="30 min · por Zoom · sin compromiso"
    />
  </div>
</main>

<script>
  const scope = document.querySelector<HTMLElement>("[data-llc-onboarding-b]");
  const form = scope?.querySelector<HTMLFormElement>("#onboarding-form-b");

  if (scope && form) {
    const activeForm = form;
    const activityInput = form.querySelector<HTMLInputElement>("#activity-b");
    const activityHint = form.querySelector<HTMLElement>("#activity-hint-b");
    const continueToContact = form.querySelector<HTMLButtonElement>(
      "#continue-to-contact-b",
    );
    const continueToSchedule = form.querySelector<HTMLButtonElement>(
      "#continue-to-schedule-b",
    );
    const status = form.querySelector<HTMLElement>("#form-status-b");
    const zcalFrame = form.querySelector<HTMLIFrameElement>("#zcal-frame-b");
    const zcalOpen = form.querySelector<HTMLAnchorElement>("#zcal-open-b");
    let submissionId = "";
    let isSubmitting = false;

    if (continueToContact) continueToContact.disabled = true;

    function showStep(step: number) {
      activeForm
        .querySelectorAll<HTMLElement>(".onboarding-step-b")
        .forEach((panel) => {
          panel.classList.toggle("hidden", Number(panel.dataset.step) !== step);
        });

      activeForm
        .querySelectorAll<HTMLElement>("[data-step-indicator]")
        .forEach((indicator) => {
          const indicatorStep = Number(indicator.dataset.stepIndicator);
          const isCurrent = indicatorStep === step;
          const isDone = indicatorStep < step;
          indicator
            .querySelector(".step-number-b")
            ?.classList.toggle("step-active-b", isCurrent);
          indicator
            .querySelector(".step-label-b")
            ?.classList.toggle("step-label-active-b", isCurrent);
          indicator
            .querySelector(".step-value-b")
            ?.classList.toggle("hidden", isDone);
          indicator
            .querySelector(".step-check-b")
            ?.classList.toggle("hidden", !isDone);
          indicator
            .querySelector(".step-number-b")
            ?.classList.toggle("step-done-b", isDone);
        });

      activeForm
        .querySelectorAll<HTMLElement>("[data-line]")
        .forEach((line) => {
          line.classList.toggle(
            "step-line-active-b",
            Number(line.dataset.line) < step,
          );
        });

      const headingSelector =
        step === 2
          ? "#contact-title-b"
          : step === 3
            ? "#schedule-title-b"
            : null;
      if (headingSelector) {
        activeForm
          .querySelector<HTMLElement>(headingSelector)
          ?.focus({ preventScroll: true });
      }

      const panel = activeForm.closest<HTMLElement>("[data-sw-card]");
      if (panel) panel.scrollTop = 0;
    }

    function setSubmitting(submitting: boolean) {
      isSubmitting = submitting;
      if (!continueToSchedule) return;
      continueToSchedule.disabled = submitting;
      continueToSchedule.textContent = submitting
        ? "Abriendo calendario..."
        : "Elegir horario";
      if (submitting) continueToSchedule.setAttribute("aria-busy", "true");
      else continueToSchedule.removeAttribute("aria-busy");
    }

    function setStatus(message: string, error = false) {
      if (!status) return;
      status.textContent = message;
      status.classList.toggle("text-red-300", error);
      status.classList.toggle("text-white/40", !error);
    }

    function validateContactFields() {
      const fields = ["#full-name-b", "#email-b", "#phone-b"]
        .map((selector) => activeForm.querySelector<HTMLInputElement>(selector))
        .filter((field): field is HTMLInputElement => Boolean(field));
      const invalidField = fields.find((field) => !field.checkValidity());
      if (!invalidField) return true;
      invalidField.reportValidity();
      invalidField.focus();
      return false;
    }

    function getSubmissionId() {
      if (submissionId) return submissionId;
      if (typeof globalThis.crypto?.randomUUID === "function") {
        submissionId = globalThis.crypto.randomUUID();
        return submissionId;
      }
      submissionId = "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(
        /[xy]/g,
        (character) => {
          const random = Math.floor(Math.random() * 16);
          const value = character === "x" ? random : (random & 0x3) | 0x8;
          return value.toString(16);
        },
      );
      return submissionId;
    }

    function buildZcalUrl(values: FormData) {
      const countryCode = String(values.get("countryCode") ?? "");
      const localPhone = String(values.get("phone") ?? "")
        .replace(/\D/g, "")
        .replace(/^0+/, "");
      const phone = `${countryCode}${localPhone}`;
      const url = new URL("https://zcal.co/t/agendar-asesoria-llc/60-USA");
      url.searchParams.set("name", String(values.get("name") ?? ""));
      url.searchParams.set("email", String(values.get("email") ?? ""));
      url.searchParams.set("smsPhone", phone);
      url.searchParams.set("a0", phone);
      url.searchParams.set("a1", String(values.get("activity") ?? ""));
      url.searchParams.set("a2", String(values.get("advice") ?? ""));
      url.searchParams.set("a3", String(values.get("question") ?? ""));
      return url.toString();
    }

    activeForm
      .querySelectorAll<HTMLButtonElement>(".activity-button-b")
      .forEach((button) => {
        button.addEventListener("click", () => {
          activeForm
            .querySelectorAll<HTMLButtonElement>(".activity-button-b")
            .forEach((option) => {
              const selected = option === button;
              option.setAttribute("aria-pressed", String(selected));
              option
                .querySelector(".activity-check-b")
                ?.classList.toggle("hidden", !selected);
              option
                .querySelector(".activity-check-wrap-b")
                ?.classList.toggle("activity-check-selected-b", selected);
            });
          if (activityInput) activityInput.value = button.dataset.value ?? "";
          if (activityHint) activityHint.textContent = "Actividad seleccionada";
          if (continueToContact) continueToContact.disabled = false;
        });
      });

    continueToContact?.addEventListener("click", () => {
      if (!activityInput?.value) return;
      showStep(2);
    });

    continueToSchedule?.addEventListener("click", () =>
      activeForm.requestSubmit(),
    );

    activeForm
      .querySelectorAll<HTMLButtonElement>(".back-button-b")
      .forEach((button) => {
        button.addEventListener("click", () =>
          showStep(Number(button.dataset.target)),
        );
      });

    activeForm.addEventListener("submit", async (event) => {
      event.preventDefault();
      if (isSubmitting) return;

      if (!validateContactFields()) {
        showStep(2);
        return;
      }

      const values = new FormData(activeForm);
      const currentSubmissionId = getSubmissionId();
      setSubmitting(true);
      setStatus("Guardando tu asesoría...");

      try {
        const response = await fetch("/api/leads", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            submissionId: currentSubmissionId,
            name: String(values.get("name") ?? ""),
            email: String(values.get("email") ?? ""),
            countryCode: String(values.get("countryCode") ?? ""),
            phone: String(values.get("phone") ?? ""),
            activity: String(values.get("activity") ?? ""),
            advice: String(values.get("advice") ?? ""),
            question: String(values.get("question") ?? ""),
            source: "tu-llc_en-usa-index-b",
            website: String(values.get("website") ?? ""),
          }),
        });

        if (!response.ok) {
          throw new Error(`Lead request failed with status ${response.status}`);
        }

        const trackingWindow = window as Window & {
          dataLayer?: Array<Record<string, unknown>>;
        };
        trackingWindow.dataLayer = trackingWindow.dataLayer || [];
        trackingWindow.dataLayer.push({ event: "lead_datos_completados" });
        try {
          localStorage.setItem("llcLeadSubmissionId", currentSubmissionId);
        } catch {
          // Storage is optional; the successful lead submission must continue.
        }

        const zcalUrl = buildZcalUrl(values);
        if (zcalFrame) zcalFrame.src = zcalUrl;
        if (zcalOpen) zcalOpen.href = zcalUrl;
        setSubmitting(false);
        showStep(3);
        scope.scrollIntoView({ behavior: "smooth", block: "start" });
      } catch {
        setStatus(
          "No pudimos guardar tu asesoría. Revisa tu conexión e inténtalo nuevamente.",
          true,
        );
        setSubmitting(false);
      }
    });

    showStep(1);
  }
</script>

<script is:inline>
  // Asegura que dataLayer exista antes de hacer el push
  window.dataLayer = window.dataLayer || [];
  window.dataLayer.push({
    event: "lead_datos_completados",
  });
</script>

<style>
  .onboarding-step-b:not(.hidden) {
    animation: step-enter-b 220ms ease-out both;
  }

  :global(.step-active-b) {
    border-color: var(--primary);
    background: var(--primary);
    color: var(--primary-foreground);
  }

  :global(.step-done-b) {
    border-color: color-mix(in srgb, var(--primary) 55%, transparent);
    background: color-mix(in srgb, var(--primary) 20%, transparent);
    color: var(--primary);
  }

  :global(.step-label-active-b) {
    color: white;
    font-weight: 600;
  }

  :global(.step-line-active-b) {
    background: color-mix(in srgb, var(--primary) 55%, transparent);
  }

  :global(.activity-check-selected-b) {
    border-color: var(--primary);
    background: var(--primary);
    color: var(--primary-foreground);
  }

  @keyframes step-enter-b {
    from {
      opacity: 0;
      transform: translateX(0.5rem);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .onboarding-step-b:not(.hidden) {
      animation: none;
    }
  }
</style>
